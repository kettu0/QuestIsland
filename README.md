"# queststore-weekend-c_of_thieves" 


To create DUMP file enter into shell:
```Shell
{
--pg_dump --username=... --password=... --dbname=queststore > '~(PATH TO YOUR REPO)\queststore-weekend-c_of_thieves\wwwroot\sql\queststore_dump.sql'
}
```
